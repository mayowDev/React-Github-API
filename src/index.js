import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
/* above line, tells to render "APP" which is main app.js
in src folder, and render into element with id "root"
which is in "index.html" file in our public folder*/
